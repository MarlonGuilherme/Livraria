<% content_for :title, "Editing book" %>

<h1><%= t('books.edit.title') %></h1>

<%= render "form", book: @book %>

<div>
  <%= link_to t('helpers.link.show'), @book %> |
  <%= link_to t('helpers.link.back'), books_path %>
</div>